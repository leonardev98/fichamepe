/** Cookie httpOnly con el JWT de refresh (misma convención que el frontend/middleware). */
export const FP_REFRESH_COOKIE = 'fp_refresh';

/** Cookie legible por JS/middleware para rutas según rol (no contiene secretos). */
export const FP_ROLE_COOKIE = 'fp_role';
